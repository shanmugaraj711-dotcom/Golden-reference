# Factory Delivery Proof

This disposable fixture proves that the Factory can publish a verified project payload into a non-default Git branch before PR delivery.

It is intentionally isolated from the Factory runtime and contains no secrets.
